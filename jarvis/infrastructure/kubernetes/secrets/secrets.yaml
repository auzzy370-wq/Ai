# IMPORTANT: In production, use external secrets manager (AWS Secrets Manager, Vault, etc.)
# This is a template - replace values before applying
apiVersion: v1
kind: Secret
metadata:
  name: jarvis-secrets
  namespace: jarvis
type: Opaque
stringData:
  database-url: "postgresql+asyncpg://jarvis:CHANGE_ME@postgres-svc:5432/jarvis"
  redis-url: "redis://redis-svc:6379/0"
  openai-api-key: "CHANGE_ME"
  anthropic-api-key: "CHANGE_ME"
  pinecone-api-key: "CHANGE_ME"
  secret-key: "CHANGE_ME_TO_STRONG_RANDOM_SECRET"
  grafana-password: "CHANGE_ME"
